{
  "name": "mcp-demo",
  "version": "1.0.0",
  "description": "MCP (Model Context Protocol) servers for weather data and Airbnb location lookup",
  "private": true,
  "workspaces": [
    "weather-server",
    "airbnb-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "start": "npm run start --workspaces",
    "clean": "npm run clean --workspaces",
    "install-all": "npm install && npm install --workspaces"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "weather",
    "airbnb",
    "rest-api"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}